@model IEnumerable<Icosoft.Models.UserType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDUserType }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDUserType }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDUserType })
        </td>
    </tr>
}

</table>
