@model IEnumerable<Icosoft.Models.DocumentType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DocumentTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DocumentTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDDocumentType }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDDocumentType }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDDocumentType })
        </td>
    </tr>
}

</table>
